from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Service for car license plate number, type, color, moving direction automatic detection',
    author='DL-group-N3',
    license='MIT',
)
